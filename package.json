{
  "name": "on-thi-gplx-600",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx wrangler deploy",
    "deploy:pages": "npm run build && npx wrangler pages deploy dist --project-name=on-thi-gplx-600"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
